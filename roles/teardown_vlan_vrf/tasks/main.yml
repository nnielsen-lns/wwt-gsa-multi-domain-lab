---
- name: Remove VLANs
  cisco.ios.ios_config:
    lines:
      - "no vlan {{ item.id }}"
  loop: "{{ vlans }}"

- name: Remove VRFs
  cisco.ios.ios_config:
    lines:
      - "no vrf definition {{ item.name }}"
  loop: "{{ vrfs }}"
  when: inventory_hostname in groups['core']
